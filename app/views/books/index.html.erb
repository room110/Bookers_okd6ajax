<div class="container">
  <div class="row">
    <%= render 'users/list', user:current_user , book:@book %>
     <div class="col-xs-9">
       <h2>Books</h2>

  <table class="table table-hover table-inverse">
        <tr>
          <th></th>
          <th></th>
          <th></th>
          <th>Title</th>
          <th></th>
          <th></th>
          <th>Opinion</th>
          <th></th>
          <th></th>
        </tr>
        <% @books.each do |book| %>
        <% @users.each do |user| %>
        <tr>
          <th>
            <%= link_to book_path(user.id) do%>
              <%= attachment_image_tag user, :profile_image, :fill, 30, 30, format: 'jpeg', class: "img-circle pull-left profile-img", fallback: "no_image.jpg" %>
            <% end %>
          </th>
          <th></th>
          <th></th>
          <th align="left"><%= link_to book.title, book_path(user.id) %></th>
          <th></th>
          <th></th>
          <th align="left"><%= book.body %></th>
          <th></th>
          <th></th>
       </tr>
     <% end %>
     <% end %>
  </table>
</div>
</div>
</div>